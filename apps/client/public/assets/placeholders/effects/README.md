Temporary effects are currently CSS-driven in styles.css.

Future production assets can be placed here as transparent sprite sheets,
short WebM clips, or PNG/WebP particles. Keep effects centered on a transparent
canvas so SkillEffectLayer can replace CSS bursts without layout changes.
